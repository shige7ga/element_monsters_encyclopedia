<p><%= t("views.devise.mailer.greeting", email: @email) %></p>

<p><%= t("views.devise.mailer.confirmation_instructions.body") %></p>

<p><%= link_to t("views.devise.mailer.confirmation_instructions.link"), confirmation_url(@resource, confirmation_token: @token) %></p>
