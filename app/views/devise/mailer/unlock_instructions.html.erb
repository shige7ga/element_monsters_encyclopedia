<p><%= t("views.devise.mailer.greeting", email: @resource.email) %></p>

<p><%= t("views.devise.mailer.unlock_instructions.body") %></p>

<p><%= t("views.devise.mailer.unlock_instructions.link_instruction") %></p>

<p><%= link_to t("views.devise.mailer.unlock_instructions.link"), unlock_url(@resource, unlock_token: @token) %></p>
